#pragma once

// Note: The location of this header file is inside "$(ProjectDir)src" not "$(ProjectDir)src/ECS".
// The purpose of this header file is to provide an easy way to bring all ECS functionality with a single #include directive

#include "ECS//S_Graphics.h"
#include "ECS/S_Physics.h"